/**
 * @swagger-enum
 */
export enum EnumOfNumbers {
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
  REDIRECT = 301
}

/**
 * @swagger-enum
 */
export enum EnumOfStrings {
  NOT_FOUND = 'A',
  SERVER_ERROR = 'B',
  REDIRECT = 'C'
}

/**
 * @swagger-enum
 */
export enum MixedEnum {
  NOT_FOUND = 'A',
  SERVER_ERROR = 500,
  REDIRECT = 'B'
}

/**
 * @swagger-enum
 */
export enum NoCustomValues {
  VALUE1,
  VALUE2,
  VALUE3
}